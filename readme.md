  cookie-parser ->The cookie-parser npm package functions as a middleware for Express.js that extracts the raw Cookie header from incoming HTTP requests, parses it into a usable JavaScript object, and exposes it via req.cookies
  cors->CORS is a Node.js middleware for Express/Connect that sets CORS response headers. These headers tell browsers which origins can read responses from your server
